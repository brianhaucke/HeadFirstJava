package ChapterFive;

public class OldDiceRollGame {
/*
            Dice dice1 = new Dice();
        Dice dice2 = new Dice();
        Dice dice3 = new Dice();
        Dice dice4 = new Dice();
        Dice dice5 = new Dice();

        int roll1 = dice1.roll();
        int roll2 = dice2.roll();
        int roll3 = dice3.roll();
        int roll4 = dice4.roll();
        int roll5 = dice5.roll();

        System.out.println(roll1);
        System.out.println(roll2);
        System.out.println(roll3);
        System.out.println(roll4);
        System.out.println(roll5);

        int sumOfAllDiceRolls = roll1 + roll2 + roll3 + roll4 + roll5;
        System.out.println("Sum of all dice: " + sumOfAllDiceRolls);

        dice1.determineYahtzee(roll1, roll2, roll3, roll4, roll5);

        DynamicDice dice4 = new DynamicDice();
        Scanner in = new Scanner(System.in);
        dice4.numberOfSides = 20;
        int roll4 = dice4.roll();
        System.out.println(roll4);


    OLD THREE OF A KIND LOGIC:
    boolean threeOfAKind =
            (((v == w) && (v == x) //&& (v != y) //&& (v != z))
                    || ((v == w) && (v == y) //&& (v != x) && (v != z))
                    || ((v == w) && (v == z) //&& (v != x) && (v != y))
                    || ((v == y) && (v == z) //&& (v != w) && (v != x))
                    || ((v == y) && (v == x) //&& (v != w) && (v != z))
                    || ((w == x)) && (w == z)
                    || ((w == x)) && (x == y)
                    || (x == y) && (y == z)
                    || (w == y) && (y == z)))))));


                boolean straight = (((v != w) && (v != x) && (v != y) && (v != z))
                && ((w != x) && (w != y) && (w != z))
                && ((x != y) && (x != z))
                && ((y != z)));

*/
}

